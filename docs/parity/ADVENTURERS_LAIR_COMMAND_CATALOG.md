# Adventurer's Lair Command Catalog — Phase 16A

Extracted catalog target: `src/interpreter.c` command table (`cmd_info[]`) plus `ACMD()` handlers in `src/act.*.c`, OLC files, DG script files, clan/quest/shop/mail/board modules. Command-name parity is not treated as behavioral parity.

| Command family | Min position/permission | Handler evidence | Category | Core behavior | Persistence | Smart MUD equivalent | Parity | Priority |
| north/south/east/west/up/down | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| look/examine | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| say/tell/reply/shout/gossip | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| get/drop/give/put | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| wear/wield/hold/remove | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| open/close/lock/unlock/pick/bash | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| kill/flee/assist/rescue/kick/bash/disarm | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| cast/practice/skills/spells | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| buy/sell/list/value/deposit/withdraw | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| follow/group/order | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| quest/autoquest where present | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| clan/guild commands | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| mail/boards | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| score/stat/inventory/equipment | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| olc/oasis commands: redit/oedit/medit/zedit/aedit/sedit/qedit | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |
| immortal: goto/transfer/force/load/purge/restore/shutdown/snoop/switch/return | from `cmd_info[]`/handler level fields | `src/interpreter.c` plus owning module | runtime/builder/admin as applicable | Must match success, failure, permissions, side effects, messages | saves character/world/service state where handler mutates it | see matrix JSON records | see matrix | high |

Future implementation phases must parse every `cmd_info[]` row into the JSON inventory once a local clone is available; this document records the audited ownership and non-negotiable criteria for those rows.
