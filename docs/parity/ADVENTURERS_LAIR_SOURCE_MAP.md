# Adventurer's Lair Source Map — Phase 16A

Baseline: remote main inspected through GitHub web because git/curl clone from container failed with CONNECT 403; local checkout unavailable. The exact repository was reachable through GitHub web, but container git/curl clone attempts failed with `CONNECT tunnel failed, response 403`; no reference files were vendored or committed.

## Build and layout evidence

GitHub tree inspection shows root build/run files (`configure`, `config.status`, `autorun*`, `CMakeLists.txt`, `Makefile*`) and source under `src/`; the README states runtime account data is created under `lib/plrfiles/accounts/` and must not be committed.

## Runtime source ownership

| File/group | Subsystem ownership | Important functions/structs | Commands/data/OLC | Behavior to preserve | Old detail not to copy | Smart MUD target |
| `src/accounts.c/h` | accounts.c/h account records and lib/plrfiles/accounts | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/interpreter.c` | interpreter.c command table and dispatch | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/act.comm.c` | act.comm.c communication | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/act.informative.c` | act.informative.c look/score/who/stat output | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/act.item.c` | act.item.c inventory/equipment/containers | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/act.movement.c` | act.movement.c movement/doors/tracking hooks | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/act.offensive.c` | act.offensive.c player combat verbs | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/act.other.c` | act.other.c skills, utility verbs, preferences | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/act.wizard.c` | act.wizard.c immortal/admin | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/fight.c` | fight.c combat/death/corpses/xp | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/handler.c` | handler.c object/char movement helpers | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/db.c/db.h` | db.c/db.h boot, world loaders, resets | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/structs.h` | structs.h core structs/flags | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/utils.h` | utils.h macros | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/class.c/class.h` | class.c/class.h class/race/level tables | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/magic.c/spell_parser.c` | magic.c/spell_parser.c spells/affects | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/spec_procs.c` | spec_procs.c shops/bank/special mobiles/rooms | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/shop.c/shop.h` | shop.c/shop.h shop runtime | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/mail.c/boards.c` | mail.c/boards.c player services | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/clan.c/act.clan.c` | clan.c/act.clan.c clans | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/quest.c/qedit.c` | quest.c/qedit.c quests | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/dg_*.c` | dg_*.c scripts/triggers | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/oasis_*.c` | oasis_*.c + *edit.c OLC | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/ai_actor*.c` | ai_actor*.c + ai_reactions.c custom AI actor/reactions | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/criticalhits.c` | criticalhits.c custom critical hits | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/asciimap.c` | asciimap.c custom map rendering | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |
| `src/classtrack.c` | classtrack.c custom class tracks | ACMD handlers, structs, loader functions, tables as applicable | `lib/world/*`, `lib/etc/*`, OLC where present | Preserve player-visible semantics from the customized file group. | Global arrays, VNUM indexing, bitvectors, direct mutation. | Canonical service + events + validated world packages. |

## Custom source areas requiring priority

`src/ai_actor.c`, `src/ai_actor_brain.c`, `src/ai_reactions.c`, `src/criticalhits.c`, `src/classtrack.c`, `src/accounts.c`, and `src/asciimap.c` are not generic command-name evidence; they indicate customized Adventurer's Lair behavior that future phases must inspect symbol-by-symbol before implementing parity.
