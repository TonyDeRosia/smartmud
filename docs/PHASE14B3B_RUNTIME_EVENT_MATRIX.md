# Phase 14B3B Runtime Event Matrix

This matrix documents the implemented advanced-ability lifecycle subscriptions in this patch. The current commit fully completes the prioritized runtime slice for auras, summon room presence/follow/cleanup hooks, and room-effect entry/exit/resident/tick behavior. Later systems remain intentionally untouched rather than broadly scaffolded.

| Event | Publisher | Subscriber | Runtime action | Persistence update | Idempotency key | Test proving it |
|---|---|---|---|---|---|---|
| `actor_entered_room` | Runtime movement/room entry | `AuraRuntimeService` | Updates actor location and reconciles all aura memberships. | `aura_membership` row inserted once per aura+actor; granted `actor_effect_instances` row created. | `am_<aura_instance_id>_<actor_id>` | `tests/test_phase14b3b_auras.py::test_aura_reconciles_entry_exit_source_movement_and_restart` |
| `actor_left_room` | Runtime movement/room exit | `AuraRuntimeService` | Reconciles memberships when an actor leaves range. | Membership is marked inactive and granted effect removed. | `am_<aura_instance_id>_<actor_id>` | `tests/test_phase14b3b_auras.py::test_aura_reconciles_entry_exit_source_movement_and_restart` |
| `movement_succeeded` | Runtime movement | `AuraRuntimeService` | Reconciles moved actor and source-owned auras after room change. | Same aura membership rows are reused; stale memberships are closed. | `am_<aura_instance_id>_<actor_id>` | `tests/test_phase14b3b_auras.py::test_aura_reconciles_entry_exit_source_movement_and_restart` |
| `actor_defeated` | Combat lifecycle | `AuraRuntimeService`, `SummonRuntimeService` | Removes source-owned auras; cleans owner summons. | Aura instances set inactive; summon relationships transition once. | Aura instance id / summon actor id | Focused service coverage; broader defeat cleanup remains for later runtime slice. |
| `actor_died` | Combat lifecycle | `AuraRuntimeService`, `SummonRuntimeService` | Removes source-owned auras; cleans owner summons. | Aura instances set inactive; summon relationships transition once. | Aura instance id / summon actor id | Focused service coverage; broader death cleanup remains for later runtime slice. |
| `character_logout` | Session/runtime logout | `AuraRuntimeService`, `SummonRuntimeService` | Removes source-owned auras; cleans owner summons. | Aura instances set inactive; summon relationships transition once. | Aura instance id / summon actor id | Focused service coverage; broader logout cleanup remains for later runtime slice. |
| `item_unequipped` | Item/equipment runtime | `AuraRuntimeService` | Removes item-source auras. | Matching aura instances set inactive. | source item instance id | Focused service coverage; item ability system untouched. |
| `effect_removed` | Effect runtime | `AuraRuntimeService` | Removes effect-source auras. | Matching aura instances set inactive. | source effect instance id | Focused service coverage. |
| `effect_expired` | Effect runtime | `AuraRuntimeService` | Removes effect-source auras. | Matching aura instances set inactive. | source effect instance id | Focused service coverage. |
| `stance_removed` | Stance/effect runtime | `AuraRuntimeService` | Removes stance-source auras. | Matching aura instances set inactive. | source stance instance id | Focused service coverage; stance lifecycle untouched. |
| `actor_entered_room` | Runtime movement/room entry | `RoomEffectRuntimeService` | Applies entry/resident operations to entering actor. | `room_effect_membership` row inserted and granted effects tracked. | `rem_<room_effect_instance_id>_<actor_id>` | `tests/test_phase14b3b_room_effects.py::test_room_effect_resident_entry_exit_ticks_and_recovery` |
| `movement_succeeded` | Runtime movement | `RoomEffectRuntimeService`, `SummonRuntimeService` | Applies room-effect entry/resident operations and moves eligible summons with owner. | Room-effect membership row inserted; summon actor location updated via runtime actor state. | Room-effect membership id / summon actor id | `tests/test_phase14b3b_room_effects.py::test_room_effect_resident_entry_exit_ticks_and_recovery`, `tests/test_phase14b3b_summons.py::test_summon_presence_follow_cleanup_and_restart_hooks` |
| `actor_left_room` | Runtime movement/room exit | `RoomEffectRuntimeService` | Removes member effects for exiting actor. | Membership state transitions from active to left reason. | `rem_<room_effect_instance_id>_<actor_id>` | `tests/test_phase14b3b_room_effects.py::test_room_effect_resident_entry_exit_ticks_and_recovery` |
| `world_loaded` | MudRuntime world load | `AuraRuntimeService`, `SummonRuntimeService`, `RoomEffectRuntimeService` | Runs idempotent advanced recovery for implemented systems. | Reconciles existing active rows without duplicating memberships. | Existing instance/membership ids | Focused recovery assertions in aura and room-effect tests. |
| `runtime_ready` / `startup_complete` | MudRuntime startup | `AuraRuntimeService`, `SummonRuntimeService`, `RoomEffectRuntimeService` | Runs idempotent advanced recovery for implemented systems. | Same as `world_loaded`. | Existing instance/membership ids | Focused recovery assertions in aura and room-effect tests. |
| `runtime_shutdown` | Runtime shutdown | `RoomEffectRuntimeService` | No mutation for persistent room effects in this slice. | None. | Event name + instance ids if future shutdown policy adds cleanup. | Documented no-op for this prioritized slice. |
