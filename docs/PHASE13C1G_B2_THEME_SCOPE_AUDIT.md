# Phase 13C1G-B2 Theme Scope Audit

| Scope | Current runtime file | Current Builder draft file | Current loader | Current validator | Current publisher | Theme assignment field | Migration needed | Tests |
|---|---|---|---|---|---|---|---|---|
| World metadata | `worlds/<world_id>/world/world.json` when present; engine fallback otherwise | `worlds/<world_id>/builder/world.json` | `engine.display_themes.load_theme_assignments` | `BuilderWorkspace.validate` | `BuilderWorkspace.export` preserves draft JSON | `default_display_theme_id`, `default_prompt_preset` | Add `world` draft collection and read it before falling back to engine defaults | Phase 13C1G-B2 focused tests |
| Zones | `worlds/<world_id>/zones/zones.json` and Builder overlay | `worlds/<world_id>/builder/zones.json` | `load_theme_assignments` plus room-to-zone lookup from `room_ids` | `BuilderWorkspace.validate` | `BuilderWorkspace.export` | `display_theme_id`, `display_theme_ids.<family>` | Preserve fields during normalization; validate references/families | Phase 13C1G-B2 focused tests |
| Areas | `worlds/<world_id>/areas/areas.json` and Builder overlay | `worlds/<world_id>/builder/areas.json` | `load_theme_assignments` plus zone `area_id` | `BuilderWorkspace.validate` | `BuilderWorkspace.export` | `display_theme_id`, `display_theme_ids.<family>` | Preserve fields during normalization; validate references/families | Phase 13C1G-B2 focused tests |
| Rooms / hierarchy | `worlds/<world_id>/rooms/rooms.json` and Builder overlay | `worlds/<world_id>/builder/rooms.json` | `BuilderWorkspace.load`, runtime world package, and `load_theme_assignments` zone `room_ids` | Existing Builder room validation | Existing Builder export | none | Use canonical room id to infer zone/area for theme resolution | Phase 13C1G-B2 precedence tests |
| Display themes | `worlds/<world_id>/display_themes/display_themes.json` | in-memory command drafts, then authored package/draft assignment references | `load_display_themes` | `validate_display_theme` plus Builder assignment validation | Existing Builder export for assignments | theme ids referenced by scopes | Unsupported executable templates continue to be rejected by validation | Existing and focused tests |

Resolution precedence is player accessibility, player explicit theme, area family, area general, zone family, zone general, world default, then engine default. Player-facing display builders consume `ResolvedDisplayTheme`; raw Builder JSON stays in validation and draft command layers.
