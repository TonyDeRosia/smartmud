# Level Up Pipeline

Level-up flow: resolve state, curve, threshold/max level, increment level, create traceable modifier/currency/ability grants, update score state, publish-compatible diagnostics, then recheck overflow XP idempotently.
